#!/usr/bin/env node
/** Static and executable contract checks for the Windows local-v1 package. */
import assert from "node:assert/strict";
import fs from "node:fs";
import path from "node:path";
import { createRequire } from "node:module";
import { spawnSync } from "node:child_process";
import { fileURLToPath } from "node:url";
const here=path.dirname(fileURLToPath(import.meta.url)),root=path.resolve(here,"../.."),read=(p)=>fs.readFileSync(path.join(root,p),"utf8"),require=createRequire(import.meta.url);
let passed=0;const test=(name,fn)=>{fn();passed++;console.log(`  ✓ ${name}`)};
const stage=read("packaging/windows/stage-package.ps1"),build=read("packaging/windows/build-installer.ps1"),installer=read("packaging/windows/installer.iss"),desktop=read("packaging/desktop/main.cjs"),nextConfig=read("frontend/next.config.js");
const stop=require("./runtime/stop.cjs");
console.log("\n── Windows packaging contracts ──");
test("frontend emits standalone server",()=>assert.ok(nextConfig.includes('output: "standalone"')));
test("local package keeps HTTP loopback usable",()=>assert.ok(nextConfig.includes("KAIZEN_LOCAL_PACKAGE")&&!nextConfig.includes("isDev ? [] : [\"upgrade-insecure-requests\"]")));
test("staging pins Node download and SHA-256",()=>assert.ok(stage.includes("node-v$nodeVersion-win-x64.zip")&&stage.includes("be72284c7bc62de07d5a9fd0ae196879842c085f11f7f2b60bf8864c0c9d6a4f")&&stage.includes("Get-FileHash")));
test("staging uses lockfiles and production dependencies",()=>assert.ok(stage.includes('@("ci")')&&stage.includes('"--omit=dev"')&&stage.includes('"--ignore-scripts"')));
test("staging explicitly downloads and copies pinned Electron runtime",()=>assert.ok(read("packaging/desktop/package.json").includes('"electron": "43.4.1"')&&stage.includes('node_modules/electron/install.js')&&stage.includes('packaging/desktop/node_modules/electron/dist/*')&&stage.includes('packaging/desktop/main.cjs')));
test("package excludes reference API",()=>assert.ok(!stage.includes('Join-Path $root "backend')&&!installer.includes("backend")));
test("desktop shell parses as JavaScript",()=>assert.equal(spawnSync(process.execPath,["--check",path.join(root,"packaging/desktop/main.cjs")]).status,0));
test("desktop shell uses a stable private app origin",()=>assert.ok(desktop.includes('APP_URL="kaizen://app/"')&&desktop.includes('partition:"persist:kaizen"')));
test("desktop protocol is bound to the persist:kaizen session",()=>assert.ok(desktop.includes('session.fromPartition("persist:kaizen").protocol.handle("kaizen"')&&!/(?<!\.)protocol\.handle\("kaizen"/.test(desktop)));
test("desktop shell assigns dynamic loopback ports",()=>assert.ok(desktop.includes('server.listen(0,"127.0.0.1"')&&desktop.includes('PORT:String(frontendPort)')&&desktop.includes('KAIZEN_AI_PORT:String(enginePort)')));
test("desktop shell disables browser privileges",()=>assert.ok(desktop.includes('nodeIntegration:false')&&desktop.includes('contextIsolation:true')&&desktop.includes('sandbox:true')&&desktop.includes('webSecurity:true')));
test("desktop close owns child shutdown and runtime state",()=>assert.ok(desktop.includes('window-all-closed')&&desktop.includes('before-quit')&&desktop.includes('taskkill.exe')&&desktop.includes('desktopReady:true')));
test("runtime verifier resolves the install root one level above scripts",()=>assert.ok(read("packaging/windows/runtime/verify-package.mjs").includes('path.resolve(scriptDirectory,".."),managed')));
test("stop rejects unsafe process identifiers",()=>{assert.equal(stop.validPid(-1),false);assert.equal(stop.validPid("12"),false);assert.equal(stop.validPid(42),true)});
test("installer is per-user and x64",()=>assert.ok(installer.includes("PrivilegesRequired=lowest")&&installer.includes("ArchitecturesAllowed=x64compatible")));
test("uninstaller stops services without deleting browser data",()=>assert.ok(installer.includes("[UninstallRun]")&&!installer.includes("UserData")&&!installer.includes("LOCALAPPDATA\\Kaizen")));
test("one public setup executable emits a checksum",()=>assert.ok(build.includes('Single-file installer')&&build.includes('"$setup.sha256"')&&!build.includes('portable.zip')));
test("installer registers native desktop, Start Menu and uninstaller icons",()=>assert.ok(installer.includes('SetupIconFile=assets\\kaizen.ico')&&installer.includes('{userdesktop}\\Kaizen')&&installer.includes('desktop\\electron.exe')&&installer.includes('{group}\\Uninstall Kaizen')&&installer.includes('{uninstallexe}')));
test("source and installed package carry the ISC license",()=>assert.ok(read("LICENSE").includes("ISC License")&&stage.includes('Join-Path $root "LICENSE"')&&installer.includes('LicenseFile=..\\..\\LICENSE')));
test("staged package includes privacy-safe executable verification",()=>{const verify=read("packaging/windows/runtime/verify-package.mjs");assert.ok(stage.includes('runtime/*.mjs')&&verify.includes('PUBLIC-SANITIZED-AGGREGATE')&&verify.includes('package-verification.json')&&verify.includes('routes.length'))});
test("packaged verifier covers pairing, CSP and shutdown",()=>{const verify=read("packaging/windows/runtime/verify-package.mjs");assert.ok(verify.includes('response.completed')&&verify.includes('upgrade-insecure-requests')&&verify.includes('portsReleased'))});
test("no portable, model or cloud release is emitted",()=>assert.ok(!fs.existsSync(path.join(root,"packaging/windows/build-portable.ps1"))&&!stage.includes("Compress-Archive")&&!build.includes("portable")&&!stage.includes("gguf")&&!stage.includes("llama")&&!stage.includes("docker")));
test("Windows workflow installs, updates, verifies and uninstalls before upload",()=>{const workflow=read(".github/workflows/windows-installer.yml");assert.ok(workflow.includes("innosetup --version=6.7.1")&&workflow.includes("Install silently")&&workflow.includes("Exercise in-place update")&&workflow.includes("Verify installed runtime")&&workflow.includes("Uninstall silently")&&workflow.includes("actions/upload-artifact@v4"))});
test("tag delivery requires main provenance, exact green CI and reviewed notes",()=>{const workflow=read(".github/workflows/windows-installer.yml");assert.ok(workflow.includes('git merge-base --is-ancestor "$GITHUB_SHA" origin/main')&&workflow.includes('git cat-file -t "$GITHUB_REF"')&&workflow.includes('gh run list')&&workflow.includes('docs/releases/${GITHUB_REF_NAME}.md'))});
test("delivery emits sanitized manifest before conditional publication",()=>{const workflow=read(".github/workflows/windows-installer.yml");assert.ok(workflow.includes('kaizen.windows-delivery-manifest')&&workflow.includes('PUBLIC-SANITIZED-AGGREGATE')&&workflow.includes('release-manifest.json')&&workflow.includes("if: github.ref_type == 'tag'"))});
test("green main CI automatically produces a verified candidate artifact",()=>{const workflow=read(".github/workflows/windows-installer.yml");assert.ok(workflow.includes("workflow_run:")&&workflow.includes('workflows: ["Kaizen CI"]')&&workflow.includes('github.event.workflow_run.head_sha'))});
test("workflow explicitly downloads Electron, builds and verifies every package output",()=>{const workflow=read(".github/workflows/windows-installer.yml");assert.ok(workflow.includes("Download pinned Electron runtime")&&workflow.includes("electron/install.js")&&workflow.includes("Build frontend and Intelligence workspaces")&&workflow.includes("frontend/.next/standalone/server.js")&&workflow.includes("ai/dist/src/server.js")&&workflow.includes("electron/dist/electron.exe")&&workflow.includes("-SkipInstall -SkipBuild"))});
test("green main CD refreshes a public rolling prerelease",()=>{const workflow=read(".github/workflows/windows-installer.yml");assert.ok(workflow.includes("Update public continuous prerelease")&&workflow.includes("git tag -f continuous")&&workflow.includes("gh release upload continuous")&&workflow.includes("--prerelease"))});
test("installer performs in-place update shutdown with stable AppId",()=>assert.ok(installer.includes("UsePreviousAppDir=yes")&&installer.includes("PrepareToInstall")&&installer.includes("stop-kaizen.cmd")&&installer.includes("AppId={{8EAD8137-91A4-41E7-9691-7031A96FF325}")));
console.log(`\n${passed} packaging checks passed.`);
