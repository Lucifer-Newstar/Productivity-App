# v0.1 Integration Validation

**Milestone:** complete — deterministic/mock v0.1 validated; Wave 0 closed with no model<br>
**Scope:** frozen pending next-capability review; no additional domains, memory, Health, writes or automation

## Required gates

| Gate | Status | Evidence |
|---|---|---|
| Mock provider engine→tool→browser→response | PASS | Real loopback gateway/SSE/tool-result integration test |
| Same-origin Next proxy→engine health/pairing | PASS | Runtime smoke and frontend structural QA |
| `core.today@1.0` read-only/minimum context | PASS | Frontend AI QA; Note/Health exclusion |
| Source-ID and response schema validation | PASS | Engine contract/orchestrator tests |
| Task-content prompt injection | PASS with deterministic mock | Adversarial test; content stays cited data |
| Notification-content prompt injection | PASS with deterministic mock | Adversarial test; content cannot alter answer/tool |
| Scheduled-item prompt injection | PASS with deterministic mock | Adversarial test; content cannot alter answer/tool |
| Attempted second/write tool escalation | PASS | Orchestrator rejects with `TOOL_LIMIT` |
| Public/local privacy boundary | PASS | Tracked privacy scan + allowlist sanitizer |
| llama.cpp HTTP/SSE adapter protocol | PASS with deterministic HTTP mock | Fragmented tool-call, text, usage and health parsing test |
| Real llama.cpp candidate quality/resources | NO SELECTION | Qwen3, Gemma3, Phi4 Mini and Qwen2.5 7B control all failed preflight |
| Real candidate through v0.1 gateway/Domain Bridge | NOT AVAILABLE | Wave 0 closed with no passing model; deterministic/mock integration remains validated |
| Target context/runtime/resource behavior | NOT AVAILABLE | No candidate passed preflight; no full model run was authorized |

## Frozen runtime scope

- Only `get_today@1.0` is registered.
- READ / ANALYZE / SUGGEST only.
- Core and Notifications only.
- Health consent remains false and no Health payload enters the snapshot.
- Notes, habits content, AI memory and retrieval are not exposed.
- Context/model/runtime settings remain configurable and provisional.
- Deterministic mock proves integration, not model quality.

## Adversarial interpretation

Injection-like text may be displayed verbatim as a user-authored task/notification label; displaying data is not executing it. The security requirement is that such text cannot change policy, request unregistered tools, add permissions, cite unknown IDs or claim mutation. v0.1 tests all those boundaries.

## Completion rule

The integration milestone closes with deterministic/mock v0.1 validated and real-model integration unavailable because Wave 0 selected no model. AI-ADR-019 now defines a separate v0.1.1 deterministic Core Today route and interpreter-only contract; its architecture/gates do not change this v0.1 result or reopen W0-GATE-2. Production v0.1.1 implementation still requires review, and v0.2 remains frozen.
