# Interpreter-model public aggregates

No model result exists yet.

Future files may enter this directory only after protocol-compliant execution, allowlist sanitization and review. Every result must declare:

```json
"classification": "PUBLIC-SANITIZED-AGGREGATE"
```

Raw prompts, outputs, paths, machine identifiers and per-sample telemetry remain under the ignored `results-local/` boundary and must never be committed or shared in public chat.