# Kaizen Intelligence Engine workspace

All Intelligence Engine work continues on the persistent `ai` branch; do not create a branch per wave. Only the disposable [Wave 0 validation harness](wave0/README.md) exists here today.

There is no production Intelligence Engine, `get_today()` implementation, model dependency, vector backend or state bridge in this workspace. Candidate technology may move forward only after the Wave 0 Selection Report is completed on the target hardware and explicitly approved.

Architecture remains under [`../docs/ai/`](../docs/ai/README.md).
